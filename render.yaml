services:
  - type: web
    name: wealth-inequality-sim
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app_wealth_inequality.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
